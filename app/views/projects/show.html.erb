<p>
	Project: <%= @project.name %>
</p>
<p>
	Issues: 
</p>
<ul>
	<% @project.issues.each do |iss| %>
		<li>
			<%= iss.title %>
			<%= link_to "Edit", edit_issue_path(iss) %>
		</li>
	<% end %>
</ul>
<%= link_to "< Back", "/" %>